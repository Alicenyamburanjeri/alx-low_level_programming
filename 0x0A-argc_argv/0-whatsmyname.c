#include <stdio.h>

/**
 * main - prints the name
 * @argc: arg counter for char
 * @argv: arg value for char
 * Return: 0
 */

int main(int argc, char *argv[])
{
	int i = argc - arg c;

	printf("%s\n", argv[i]);

	return (0);
}
